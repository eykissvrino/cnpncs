[build]
builder = "NIXPACKS"
buildCommand = "npx prisma generate && npx next build"

[deploy]
startCommand = "mkdir -p /data && npx prisma db push || true && exec node_modules/.bin/next start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
