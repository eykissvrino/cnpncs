[build]
builder = "NIXPACKS"
buildCommand = "npx prisma generate && npx next build"

[deploy]
startCommand = "mkdir -p /data && node prisma/migrate.mjs && exec node_modules/.bin/next start -p ${PORT:-3000}"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
