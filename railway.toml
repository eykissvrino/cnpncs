[build]
builder = "NIXPACKS"
buildCommand = "npx prisma generate && npx next build"

[deploy]
startCommand = "node prisma/migrate.mjs && exec node_modules/.bin/next start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
