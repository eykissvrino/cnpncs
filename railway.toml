[build]
builder = "NIXPACKS"
buildCommand = "DATABASE_URL=file:./dev.db npx prisma generate && npx next build"

[deploy]
startCommand = "mkdir -p /data && node prisma/migrate.mjs && exec node_modules/.bin/next start -H 0.0.0.0 -p 3000"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
